#include "mainwindow.h"
#include "glwidget.h"
#include "transdow.hpp"
#include "hcore.hpp"

namespace GUI{

    extern DetectionWindow *dwin;
    extern MainWindow *mw;
    extern HCore::HCore *m_core;
    extern TransWindow *tw;

    void init(HCore::HCore *core);
}