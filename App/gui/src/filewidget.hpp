#include <QWidget>
#include "mainwindow.h"

namespace GUI{

    class FileWidget : public QWidget{
        Q_OBJECT
    public:
        FileWidget(MainWindow *main);
    private slots:
    private:
    };

}